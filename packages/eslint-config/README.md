# ESLint Config (`@repo/eslint-config`)

Configurations ESLint partagées pour le projet Lighthouse.

- `library.js`: Pour les packages TypeScript génériques.
- `next.js`: Pour les applications Next.js.
- `react-internal.js`: Pour les packages React internes.
- `server.js`: Pour les serveurs Node.js/Express.
