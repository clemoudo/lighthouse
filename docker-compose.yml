services:
  traefik:
    image: traefik:v3.6
    container_name: traefik
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./infra/traefik/letsencrypt:/letsencrypt
      - ./infra/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      web:
        condition: service_healthy
      api:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik-dashboard.rule=Host(`traefik.${BASE_DOMAIN}`)"
      - "traefik.http.routers.traefik-dashboard.entrypoints=websecure"
      - "traefik.http.routers.traefik-dashboard.tls=true"
      - "traefik.http.routers.traefik-dashboard.tls.certresolver=letsencrypt"
      - "traefik.http.routers.traefik-dashboard.service=api@internal"
      - "traefik.http.routers.traefik-dashboard.middlewares=traefik-auth"
      # BasicAuth for dashboard: user:password (use htpasswd to generate)
      - "traefik.http.middlewares.traefik-auth.basicauth.users=${TRAEFIK_DASHBOARD_AUTH}"
      - "traefik.docker.network=gateway"
    networks:
      - proxy-only
      - gateway

  postgres:
    image: ${POSTGRES_IMAGE:-ghcr.io/clemoudo/lighthouse-postgres:latest}
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: "${DB_USER}"
      POSTGRES_PASSWORD: "${DB_PASSWORD}"
      POSTGRES_DB: "${DB_NAME}"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - backend

  web:
    container_name: web
    image: ${WEB_IMAGE:-ghcr.io/clemoudo/lighthouse-web:latest}
    restart: unless-stopped
    environment:
      - NODE_ENV=${NODE_ENV}
      - CI=true
      - INFISICAL_PROJECT_ID=${INFISICAL_PROJECT_ID}
      - INFISICAL_API_URL=${INFISICAL_API_URL}
      - INFISICAL_CLIENT_ID=${INFISICAL_CLIENT_ID_WEB}
      - INFISICAL_CLIENT_SECRET=${INFISICAL_CLIENT_SECRET_WEB}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://web:${WEB_PORT}"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.web.rule=Host(`${BASE_DOMAIN}`)"
      - "traefik.http.routers.web.entrypoints=websecure"
      - "traefik.http.routers.web.tls=true"
      - "traefik.http.routers.web.tls.certresolver=letsencrypt"
      - "traefik.http.services.web.loadbalancer.server.port=${WEB_PORT}"
      - "traefik.docker.network=gateway"
      # Security Middlewares
      - "traefik.http.routers.web.middlewares=sec-headers"
      - "traefik.http.middlewares.sec-headers.headers.browserXssFilter=true"
      - "traefik.http.middlewares.sec-headers.headers.contentTypeNosniff=true"
      - "traefik.http.middlewares.sec-headers.headers.frameDeny=true"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - gateway

  api:
    container_name: api
    image: ${API_IMAGE:-ghcr.io/clemoudo/lighthouse-api:latest}
    restart: unless-stopped
    environment:
      - NODE_ENV=${NODE_ENV}
      - CI=true
      - INFISICAL_PROJECT_ID=${INFISICAL_PROJECT_ID}
      - INFISICAL_API_URL=${INFISICAL_API_URL}
      - INFISICAL_CLIENT_ID=${INFISICAL_CLIENT_ID_API}
      - INFISICAL_CLIENT_SECRET=${INFISICAL_CLIENT_SECRET_API}
      - UPLOAD_DIR=/app/uploads
    volumes:
      - document_storage:/app/uploads
    healthcheck:
      test: ["CMD", "curl", "-f", "http://api:${API_PORT}/status"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api.rule=Host(`${BASE_DOMAIN}`) && PathPrefix(`/api`)"
      - "traefik.http.routers.api.entrypoints=websecure"
      - "traefik.http.routers.api.tls=true"
      - "traefik.http.routers.api.tls.certresolver=letsencrypt"
      - "traefik.http.middlewares.api-strip.stripprefix.prefixes=/api"
      - "traefik.http.routers.api.middlewares=api-strip,sec-headers"
      - "traefik.http.services.api.loadbalancer.server.port=${API_PORT}"
      - "traefik.docker.network=gateway"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - gateway
      - backend

  uptime-kuma:
    image: louislam/uptime-kuma:2
    container_name: uptime-kuma
    restart: unless-stopped
    volumes:
      - uptime_kuma_data:/app/data
    networks:
      - gateway
    healthcheck:
      test: ["CMD-SHELL", "node extra/healthcheck.js"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 60s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.uptime-kuma.rule=Host(`status.${BASE_DOMAIN}`)"
      - "traefik.http.routers.uptime-kuma.entrypoints=websecure"
      - "traefik.http.routers.uptime-kuma.tls=true"
      - "traefik.http.routers.uptime-kuma.tls.certresolver=letsencrypt"
      - "traefik.http.services.uptime-kuma.loadbalancer.server.port=3001"
      - "traefik.docker.network=gateway"

networks:
  proxy-only:
    name: proxy-only
    driver: bridge
  gateway:
    name: gateway
    driver: bridge
  backend:
    name: backend
    driver: bridge
    internal: true

volumes:
  postgres_data:
  document_storage:
  uptime_kuma_data:
